<%= form_for(current_person.active_relationships.build) do |f| %>
  
  <div>
    <%= hidden_field_tag :followed_id, @person.id %>
  </div>
  
  <%= f.submit "Follow", class: "btn btn-primary" %>

<% end %>
