<%= form_for(current_person.active_relationships.
             find_by(followed_id: @person.id), 
             html: { method: :delete }, remote: true) do |f| %>
             
  <%= f.submit "Unfollow", class: "btn" %>
  
<% end %>