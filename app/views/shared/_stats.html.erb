<% @person ||= current_person %>
<div class="stats">
  <a href="<%= following_person_path(@person) %>">
    <strong id="following" class="stat">
      <%= @person.following.count %>
    </strong>
    following
  </a>
  <a href="<%= followers_person_path(@person) %>">
    <strong id="followers" class="stat">
      <%= @person.followers.count %>
    </strong>
    followers
  </a>
</div>
